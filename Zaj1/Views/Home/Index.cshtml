@using System.Collections
@model IEnumerable<Zaj1.Models.Kategoria>
@{
    ViewData["Title"] = "Home Page";
}

<a class="btn btn-success" href="@Url.Action("DodajFilm","Filmy")">dodaj Film</a>
<a class="btn btn-success" href="@Url.Action("Index","Koszyk")">Pokaż koszyk</a>

<div class="text-center">
    @if (Model != null)
    {
        foreach (var kategorie in Model)
        {
                    <p></p>
        }
    }
    else
    {
            <p>Brak dostępnych kategorii.</p>
    }
</div>


